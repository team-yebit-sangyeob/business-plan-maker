# Python 3.11

# --- Vector DB (infra, B) ---
chromadb>=0.5
langchain-chroma>=0.1
langchain-community>=0.3
langchain-openai>=0.3.24  # reasoning_effort 생성자 지원(call_json 추론 강도 노브)
langchain-text-splitters>=0.3
pypdf>=4.0

# --- Orchestrator / API server (A) ---
fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.1
pydantic>=2.7
langgraph>=0.2
langchain-core>=0.3
python-dotenv>=1.0

# --- 계획서 PDF 렌더 (12장 Block 2) ---
# markdown→HTML→PDF. weasyprint는 시스템 라이브러리(pango·cairo·gdk-pixbuf) 필요:
#   macOS: brew install pango gdk-pixbuf
weasyprint>=62
markdown>=3.6

# --- Research cluster (A) — web fact verification ---
openai>=1.40
tavily-python>=0.5

# --- Observability — LangSmith 트레이싱 ---
# wrap_openai/@traceable로 리서치 LLM·검색 호출을 LangSmith에 기록.
# langgraph의 전이 의존성이지만 직접 import하므로 명시한다.
langsmith>=0.1
